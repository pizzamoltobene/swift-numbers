# Apple Numbers AppleScript Capability Map

This file is generated by `swift run swiftnumbers refresh-apple-numbers-map`.
It is a planning oracle only; shipped SwiftNumbers library code does not depend on Numbers.app.

## Snapshot Metadata

- Oracle status: available
- Reason: Numbers.app discovered through LaunchServices/AppleScript and sdef parsed.
- Discovery method: AppleScript LaunchServices: path to application id "com.apple.Numbers"
- Numbers application: com.apple.Numbers (LaunchServices-discovered path redacted for deterministic diffs)
- sdef status: parsed
- sdef byte count: 45683
- Timestamp policy: omitted for deterministic diffs

## Capability Summary

| Area | AppleScript status | Evidence | SwiftNumbers planning target |
|---|---|---|---|
| document-lifecycle | available | `save`, `export` | Open/save/export parity and deterministic document metadata. |
| sheet-read | available | `sheet` | Sheet names, IDs, ordering, and lookup behavior. |
| table-read | available | `table` | Table names, IDs, dimensions, summaries, and selection semantics. |
| cell-range-read | available | `cell`, `range`, `row`, `column`, `value` | Cell/range/row/column read semantics and typed value parity. |
| mutation | available | `make`, `delete`, `move`, `row`, `column`, `table` | Safe sheet/table/row/column mutation behavior. |
| formula | available | `formula` | Formula text/result read behavior and guarded formula writes. |
| formatting-style | available | `format`, `font`, `alignment`, `color`, `background` | Formatting, style, rich text, and display-value parity. |
| chart | available | `chart` | Chart object discovery and safe read-only diagnostics. |
| pivot | missing | <none> | Pivot object discovery, diagnostics, and safe write blocking. |
| media-shape | available | `image`, `movie`, `audio`, `shape`, `line` | Media/shape object inventory and unsupported-object diagnostics. |

## Read Semantics Probe Rows

These rows keep AppleScript/OSAScript read semantics visible to the roadmap conveyor.
They are generated from the local Numbers scripting dictionary or deterministic skipped metadata.

| Probe | AppleScript status | Evidence | SwiftNumbers public surface |
|---|---|---|---|
| sheet-list-read | available | `sheet`, `sheet.name` | `NumbersDocument.sheetSummaries(at:)`, `swiftnumbers list-sheets` |
| table-list-read | available | `table`, `table.name`, `table.row count`, `table.column count` | `NumbersDocument.tableSummaries(at:)`, `swiftnumbers list-tables` |
| range-read | available | `range`, `range.cell`, `range.row`, `range.column` | `NumbersDocument.readRange(...)`, `swiftnumbers read-range` |
| row-read | available | `row`, `row.address`, `row.height` | `TableModel.readCells(in:)`, `swiftnumbers read-range` row slices |
| column-read | available | `column`, `column.address`, `column.width` | `TableModel.readCells(in:)`, `swiftnumbers read-column` |
| cell-value-read | available | `cell`, `cell.value`, `cell.formatted value` | `NumbersDocument.readCell(...)`, `swiftnumbers read-cell` |
| table-selection-range-read | available | `table`, `table.selection range`, `table.cell range` | `TableModel.usedRange`, table/range diagnostics |

## Mutation Semantics Probe Rows

These rows keep AppleScript/OSAScript document, sheet, table, row, and column mutation semantics visible to the roadmap conveyor.
They map Apple-visible write operations to SwiftNumbers supported APIs or safe-write backlog gaps.

| Probe | AppleScript status | Evidence | SwiftNumbers parity target |
|---|---|---|---|
| document-export-operation | available | `export` | safe export is CLI-level (`export-csv`); native document export remains a roadmap gap |
| sheet-create-delete-operation | available | `make`, `delete`, `sheet` | `EditableNumbersDocument.addSheet`; sheet delete/rename parity remains a backlog gap |
| table-create-delete-operation | available | `make`, `delete`, `table` | `EditableSheet.addTable`; table delete/duplicate parity remains a backlog gap |
| row-mutation-operation | available | `add row above`, `add row below`, `delete`, `row` | `appendRow`, `insertRow`, `deleteRow` with grouped/pivot-linked safety guards |
| column-mutation-operation | available | `add column before`, `add column after`, `delete`, `column` | `appendColumn`, `deleteColumn`; `insertColumn` has a deterministic unsupported guard until native column shifting lands |
| cell-range-clear-set-operation | available | `clear`, `set`, `cell`, `range` | `setValue`, `clearValue`, `clearValues`; range fill parity remains a safe-write backlog gap |
| table-structure-transform-operation | available | `merge`, `unmerge`, `sort`, `transpose` | `mergeCells`, `unmergeCells`; sort/transpose parity remains a backlog gap |

## Formula Semantics Probe Rows

These rows keep AppleScript/OSAScript formula text, result, range-reference, function-call, and write semantics visible to the roadmap conveyor.
They map Apple-visible formula operations to SwiftNumbers formula read APIs and guarded formula-write behavior.

| Probe | AppleScript status | Evidence | SwiftNumbers parity target |
|---|---|---|---|
| formula-text-read | available | `cell`, `cell.formula` | `TableModel.formula(...)`, `TableModel.formulas()`, `swiftnumbers list-formulas` |
| formula-result-read | available | `cell`, `cell.formula`, `cell.value`, `cell.formatted value` | `TableModel.formulaResult(...)`, `NumbersDocument.readCell(...)` formula result payloads |
| formula-range-reference-read | available | `cell.formula`, `range`, `range.cell` | formula AST/range-reference rendering and `--formulas` parity output |
| formula-function-call-read | available | `cell.formula`, `table`, `table.cell` | formula AST function-call rendering with deterministic fallback summaries |
| formula-set-operation | available | `set`, `cell`, `cell.formula` | `EditableTable.setValue(.formula(...))` with strict unsafe-reference guards |
| formula-clear-operation | available | `clear`, `cell`, `cell.formula` | formula clear is covered by `clearValue`/`clearValues`; richer formula write parity remains a backlog gap |

## Advanced Object Discovery Probe Rows

These rows keep AppleScript/OSAScript chart, pivot, style, media, shape, and text-object surfaces visible to the roadmap conveyor.
Each row classifies the current SwiftNumbers stance as supported, safe-read-only, safe-write-blocked, or unsupported.

| Probe | AppleScript status | Evidence | SwiftNumbers classification | SwiftNumbers parity target |
|---|---|---|---|---|
| chart-object-discovery | available | `chart` | safe-read-only | chart inventory and read-only diagnostics; native chart writes remain unsupported |
| pivot-object-discovery | missing | missing `pivot` | safe-write-blocked | pivot-like object diagnostics and write blocking for linked analytical drawables |
| range-formatting-style-discovery | available | `range`, `range.format`, `range.font name`, `range.font size`, `range.background color`, `range.text color`, `range.alignment`, `range.vertical alignment` | safe-read-only | `ReadCell.style`, `ReadCell.formatted`, `formattedValue(...)`, CLI `--formatting`; richer write parity remains guarded |
| media-object-discovery | available | `image`, `movie`, `audio clip` | safe-read-only | media object inventory with unsupported-object diagnostics |
| shape-line-text-object-discovery | available | `shape`, `line`, `text item` | safe-read-only | shape/line/text object inventory and no unsafe native mutation support |
| rich-text-style-discovery | available | `rich text`, `rich text.color`, `rich text.font`, `rich text.size` | safe-read-only | `ReadCell.richText`, `ReadCell.style`, `formattedValue(...)`; richer rich-text writes remain a backlog gap |

## Scripting Dictionary Inventory

- Suites: 4
- Commands: 16
- Classes: 21
- Enumerations: 9

### Suites

- `Compatibility Suite`
- `Numbers Suite`
- `iWork Suite`
- `iWork Text Suite`

### Commands

- `add column after`
- `add column before`
- `add row above`
- `add row below`
- `clear`
- `delete`
- `export`
- `make`
- `merge`
- `remove`
- `remove password`
- `set`
- `set password`
- `sort`
- `transpose`
- `unmerge`

### Classes

- `sheet` (suite: Numbers Suite)
  - properties: name
  - elements: -
- `template` (suite: Numbers Suite)
  - properties: id, name
  - elements: -
- `audio clip` (suite: iWork Suite)
  - properties: clip volume, file name, repetition method
  - elements: -
- `cell` (suite: iWork Suite)
  - properties: column, formatted value, formula, row, value
  - elements: -
- `chart` (suite: iWork Suite)
  - properties: -
  - elements: -
- `column` (suite: iWork Suite)
  - properties: address, width
  - elements: -
- `group` (suite: iWork Suite)
  - properties: height, parent, position, rotation, width
  - elements: -
- `iWork container` (suite: iWork Suite)
  - properties: -
  - elements: audio clip, chart, group, iWork item, image, line, movie, shape, table, text item
- `iWork item` (suite: iWork Suite)
  - properties: height, locked, parent, position, width
  - elements: -
- `image` (suite: iWork Suite)
  - properties: description, file, file name, opacity, reflection showing, reflection value, rotation
  - elements: -
- `line` (suite: iWork Suite)
  - properties: end point, reflection showing, reflection value, rotation, start point
  - elements: -
- `movie` (suite: iWork Suite)
  - properties: file name, movie volume, opacity, reflection showing, reflection value, repetition method, rotation
  - elements: -
- `range` (suite: iWork Suite)
  - properties: alignment, background color, font name, font size, format, name, text color, text wrap, vertical alignment
  - elements: cell, column, row
- `row` (suite: iWork Suite)
  - properties: address, height
  - elements: -
- `shape` (suite: iWork Suite)
  - properties: background fill type, object text, opacity, reflection showing, reflection value, rotation
  - elements: -
- `table` (suite: iWork Suite)
  - properties: cell range, column count, footer row count, header column count, header row count, name, row count, selection range
  - elements: cell, column, range, row
- `text item` (suite: iWork Suite)
  - properties: background fill type, object text, opacity, reflection showing, reflection value, rotation
  - elements: -
- `character` (suite: iWork Text Suite)
  - properties: -
  - elements: -
- `paragraph` (suite: iWork Text Suite)
  - properties: -
  - elements: character, word
- `rich text` (suite: iWork Text Suite)
  - properties: color, font, size
  - elements: character, paragraph, word
- `word` (suite: iWork Text Suite)
  - properties: -
  - elements: character

## Refresh Commands

```sh
swift run swiftnumbers refresh-apple-numbers-map
swift run swiftnumbers refresh-apple-numbers-map --dry-run
swift run swiftnumbers refresh-apple-numbers-map --skip-oracle --dry-run
```
